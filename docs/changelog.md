# Changelog

- [Added]: "Push to Obsidian" button on dashboard — manual one-way push of transcriptions to Obsidian vault with speaker names applied, recording tags in YAML frontmatter, and AI enhancement sections (summary, key points, action items as checkboxes) (josh, 2026-03-25)
- [Changed]: Auto-push to Obsidian after saving speaker names — silently pushes if configured, silently skips if not (josh, 2026-03-25)
- [Fixed]: Obsidian vault path no longer prepends redundant date to note filename — uses recording title directly since filenames already contain dates (josh, 2026-03-25)
- [Changed]: Docker Compose app service now includes `extra_hosts: host.docker.internal` for Obsidian REST API connectivity from container to host (josh, 2026-03-25)
- [Changed]: Gemini diarization prompt no longer includes a speaker count hint — Gemini now auto-detects all distinct speakers without any cap (josh, 2026-03-25)
- [Added]: Speaker name mapping — UI to replace generic "Speaker 1/2/3" labels with real names, stored per-transcription as JSONB, applied to display and exports; includes collapsible editor with color-coded speakers and save button (josh, 2026-03-24)
- [Changed]: Speaker count dropdown extended from 2-4 to 2-10; Gemini prompt now treats count as a hint and detects all distinct speakers without merging (josh, 2026-03-24)
- [Fixed]: Gemini diarized transcription now guaranteed to have blank lines between speaker turns via post-processing (`ensureSpeakerBlankLines`) — no longer depends on LLM formatting compliance (josh, 2026-03-24)
- [Added]: Upstream deletion detection — sync now flags recordings deleted from Plaud with a "Local only" badge (CloudOff icon) instead of auto-removing them; users can manually delete via trash icon (josh, 2026-03-24)
- [Added]: DELETE /api/recordings/[id] route to permanently remove a recording and its stored audio (josh, 2026-03-24)
- [Changed]: Dashboard now filters out trashed recordings (isTrash=true) from the main view (josh, 2026-03-24)
- [Added]: User-defined multi-tag system for categorizing recordings — DB tables, CRUD API routes, tag assignment popover in right panel, filter pills in recording list, and tag management in Settings (josh, 2026-03-24)
- [Fixed]: Rename now optimistically updates the UI title and recording list immediately, no more stale name after saving (josh, 2026-03-24)
- [Changed]: Removed duplicate recording title from inside the audio player card, keeping only the editable title above with the pencil icon (josh, 2026-03-24)
- [Added]: Rename (pencil icon) and re-transcribe (refresh icon) buttons added directly to the dashboard workstation view — no need to navigate away from the main page (josh, 2026-03-24)
- [Fixed]: Enhancement and title generation now skip Google Gemini provider (incompatible with chat completions), auto-select LiteLLM/OpenAI-compatible provider, and use configurable `ENHANCEMENT_CHAT_MODEL` instead of hardcoded `gpt-4o-mini` (josh, 2026-03-24)
- [Changed]: Replaced Google Speech-to-Text provider with Gemini multimodal (Vertex AI) — sends audio directly to Gemini for speaker-labeled transcription, no GCS upload needed, no inline-audio size limits (josh, 2026-03-24)
- [Changed]: Renamed UI provider from "Google Speech" to "Google Gemini" with default model `gemini-2.5-flash` in add/edit dialogs and factory inference (josh, 2026-03-24)
- [Fixed]: Added automatic fallback from Google provider to non-Google transcription providers when the primary Google call fails (josh, 2026-03-24)
- [Added]: Implemented Google Speech diarization provider, wired provider inference/factory routing, and added runtime-only Docker credential mount/env configuration for local service-account auth (josh, 2026-03-24)
- [Changed]: Updated fork state tracking with current remaining work and planned Google Speech diarization feature scope/config requirements (josh, 2026-03-24)
- [Added]: Local speaker diarization toggle and speaker-count settings wired into transcription flow with `diarized_json` request mode and automatic fallback to `verbose_json` when provider support is unavailable (josh, 2026-03-24)
- [Fixed]: Refactored `/api/recordings/[id]/transcribe` to use the provider abstraction path, enabling successful Azure Whisper transcription via LiteLLM for Plaud audio (josh, 2026-03-24)

- [Added]: Obsidian settings UI section (API URL, key, vault path, auto-export, test connection), recording workstation enhanced with inline rename/enhance panel/Obsidian export, DB migration 0011, docker-compose updated to build from fork source, unit tests for audio format detection and provider factory (josh, 2026-03-23)

- [Added]: Recording rename API route (`PATCH /api/recordings/[id]/rename`) with Plaud cloud sync-back support (josh, 2026-03-24)
- [Added]: Obsidian integration library (`src/lib/obsidian/`) with REST API client and Markdown formatter including YAML frontmatter generation (josh, 2026-03-24)
- [Added]: Obsidian export API route (`POST /api/recordings/[id]/export-obsidian`) to push transcriptions as formatted notes to Obsidian vaults (josh, 2026-03-24)
- [Added]: AI enhancements API route (`POST /api/recordings/[id]/enhance`) for generating summaries, action items, and key points from transcriptions using OpenAI-compatible providers (josh, 2026-03-24)
- [Changed]: Recording detail route (`GET /api/recordings/[id]`) now returns `enhancement` alongside `recording` and `transcription` (josh, 2026-03-24)
